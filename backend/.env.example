# Copy to .env and fill in your values: cp .env.example .env

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/maison_sprint3?schema=public"
JWT_SECRET="change-this-to-a-random-secret-string-at-least-32-chars"
PORT=4000

# Email (Story 17) — leave blank to use Ethereal test account (preview URL in logs)
# To use real SMTP, fill all four fields:
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="MAISON <noreply@maison.local>"
